# === Groq API ===
GROQ_API_KEY=groq_your_key_here

# === Database (optional — app uses a JSON file store if unset) ===
# DATABASE_URL=postgresql://user:pass@host:5432/db

# === Auth (single-user password) ===
DASHBOARD_PASSWORD=change_me_strong_password

# === Optional: LinkedIn API ===
LINKEDIN_ACCESS_TOKEN=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# === Image Generation ===
IMAGE_PROVIDER=pollinations
TOGETHER_API_KEY=
